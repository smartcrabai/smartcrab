/target
.env*
/testcrab/
node_modules/
.claude/scheduled_tasks.lock

.build/

# Bun service binary (built by CI, staged into the .app bundle)
apps/macos/Resources/smartcrab-service

# Rust seher-bridge sidecar binary (built by CI, staged into the .app bundle)
apps/macos/Resources/seher-bridge

